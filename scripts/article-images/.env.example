# Copy this file to `.env` in the REPO ROOT (not in this scripts/ folder)
# and paste your real key in. `.env` is gitignored so it never gets
# committed.
GEMINI_API_KEY=your-key-here

# Optional: override the image model (defaults to gemini-3-pro-image).
# GEMINI_IMAGE_MODEL=gemini-3-pro-image
